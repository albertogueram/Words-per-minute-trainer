import tkinter as tk
from objects import TypingSpeedApp

# Create the main window
root = tk.Tk()

# Create an instance of the TypingSpeedApp class
app = TypingSpeedApp(root)

# Run the Tkinter event loop
root.mainloop()
